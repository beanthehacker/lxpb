node_modules/
.next/
next-env.d.ts
.env
.env.local
.env*.local
__pycache__/
*.pyc
*.log

# Reports too large to commit raw (>GitHub's 100MB blob limit) are committed
# as public/reports/**/*.html.gz instead and inflated by
# scripts/decompress-gz-reports.mjs (runs before dev/build) -- the inflated
# .html itself is a build artifact, not source, so list each one here.
public/reports/ss_m5_confl2/ss_m5_confl2_report_2026_full_year.html
